<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="AppBaseTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource App.Brush.TextPrimary}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
    </Style>

    <Style x:Key="AppTitleTextStyle"
           BasedOn="{StaticResource AppBaseTextStyle}"
           TargetType="TextBlock">
        <Setter Property="FontSize" Value="28" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="SectionTitleTextStyle"
           BasedOn="{StaticResource AppBaseTextStyle}"
           TargetType="TextBlock">
        <Setter Property="FontSize" Value="18" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="CardTitleTextStyle"
           BasedOn="{StaticResource AppBaseTextStyle}"
           TargetType="TextBlock">
        <Setter Property="FontSize" Value="15" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="BodyTextStyle"
           BasedOn="{StaticResource AppBaseTextStyle}"
           TargetType="TextBlock">
        <Setter Property="FontSize" Value="14" />
    </Style>

    <Style x:Key="LabelTextStyle"
           BasedOn="{StaticResource AppBaseTextStyle}"
           TargetType="TextBlock">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Foreground" Value="{DynamicResource App.Brush.TextMuted}" />
    </Style>

    <Style x:Key="ValueTextStyle"
           BasedOn="{StaticResource AppBaseTextStyle}"
           TargetType="TextBlock">
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="CaptionTextStyle"
           BasedOn="{StaticResource AppBaseTextStyle}"
           TargetType="TextBlock">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{DynamicResource App.Brush.TextMuted}" />
    </Style>

    <Style x:Key="InlineStatusTextStyle"
           BasedOn="{StaticResource AppBaseTextStyle}"
           TargetType="TextBlock">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{DynamicResource App.Brush.Warning}" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="StatusPillTextStyle"
           BasedOn="{StaticResource AppBaseTextStyle}"
           TargetType="TextBlock">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{DynamicResource App.Brush.Warning}" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="LogTextStyle"
           BasedOn="{StaticResource AppBaseTextStyle}"
           TargetType="TextBlock">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Foreground" Value="{DynamicResource App.Brush.TextSecondary}" />
    </Style>
</ResourceDictionary>
